<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users List</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.1.1/css/all.css"
    integrity="sha384-O8whS3fhG2OnA5Kas0Y9l3cfpmYjapjI0E4theH4iuMD+pLhbf6JI0jIMfYcK3yZ" crossorigin="anonymous">
   
    <style>
        /* Custom styles */
        body {
            background-color: #f8f9fa;
            padding: 20px;
        }

        table {
            box-sizing: border-box;
            width: 100%;
        }

        th {
            background-color: #007bff;
            color: #fff;
        }
        
        th, td {
            text-align: center;
            vertical-align: middle;
        }

        th, td img {
            max-width: 50%;
            max-height: 20%;
            height: auto;
            border-radius: 5px;
        }

        .btn-primary {
            margin-top: 10px;
        }

        .del {
            animation: roll 3s infinite;
            background: linear-gradient(to right bottom, blue, green, red, yellow);
            color: azure;
        }

        img {
            border-image-repeat: url('chai.jpg');
        }

        /* Animations */
        @keyframes roll {
            0% {
                font-family: Arial, Helvetica, sans-serif;
                font-weight: 800;
                font-style: oblique;
                color: rgb(247, 243, 42);
            }
            50% {
                font-family: Verdana, Geneva, Tahoma, sans-serif;
                font-weight: 500;
                font-style: initial;
                background: linear-gradient(to left yellow, orange, rgb(255, 40, 75), rgb(79, 230, 79));
                color: rgb(178, 61, 233);
                transform: translateX(-50%);
            }
            100% {
                font-family: 'Times New Roman', Times, serif;
                font-weight: 900;
                font-style: inherit;
                background: linear-gradient(to left yellow, orange, rgb(255, 40, 75), rgb(79, 230, 79));
                color: rgb(89, 230, 193);
                transform: translateX(50%);
            }
        }

        /* Additional animations */
        @keyframes fadeInUp {
            0% {
                opacity: 0;
                transform: translateY(20px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .export-btn {
            animation: fadeInUp 1s;
        }

        @keyframes fadeInDown {
            0% {
                opacity: 0;
                transform: translateY(-20px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .add-user-btn {
            animation: fadeInDown 1s;
        }
    </style>    
</head>
<body>
    <div class="table-responsive-md">
        <table class="table table-striped table-hover table-bordered table-dark align-middle">
            <thead class="table-light">
                <caption>Users List</caption>
                <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Mobile</th>
                    <th>Image</th>
                    <th>Verified</th>
                    <th>Action</th>
                    <th>Remove</th>
                </tr>
            </thead>
            <tbody class="table-group-divider table-bordered table-hover">
                <% if (data.length > 0) { %>
                    <% for (let i = 0; i < data.length; i++) { %>
                        <tr>
                            <td><%= data[i].name %></td>
                            <td><%= data[i].email %></td>
                            <td><%= data[i].mobile %></td>
                            <td>
                                <img src="images/<%= data[i].image %>"  alt="">
                            </td>
                            <td>
                                <% if (data[i].is_varified == 1) { %>
                                    <h4>Verified</h4>
                                <% } else { %>
                                    <h4>Not verified</h4>
                                <% } %>
                                
                            </td>
                        <td><a href="/ums/admin/edit-user?id=<%= data[i]._id %>" class="del">Edit</a></td>
                        <td><a href="/ums/admin/delete-user?id=<%= data[i]._id %>" class="del">Delete</a></td>

                        </tr>
                    <% } %>
                <% } else { %>
                    <tr>
                        <td colspan="5">Users not found</td>
                    </tr>
                <% } %>
            </tbody>
            <tfoot></tfoot>
        </table>
    </div>

    <a href="/ums/admin/export-pdf" class="btn btn-primary">Export data</a>
    <a href="/ums/admin/home" class="btn btn-primary">Go to home</a>
    <a href="/ums/admin/new-user" class="btn btn-primary">Add new user</a>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm"
    crossorigin="anonymous"></script>
</body>
</html>
