<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <!-- Include Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        /* Add your custom styles here (if any) */
    </style>
</head>

<body class="bg-gray-100">
    <div class="container mx-auto px-4">
        <h2 class="text-3xl font-semibold text-center my-8">Reset Password</h2>
        <% if (typeof message !== 'undefined') { %>
            <h4><%= message %></h4>
        <% } %>
        <form action="/forget" method="post" class="mx-auto max-w-sm">
            <div class="mb-4">
                <label for="Email" class="block mb-2 font-medium">Email</label>
                <input type="email" class="w-full px-4 py-2 rounded border border-gray-400 focus:border-blue-500 focus:outline-none" name="email" required aria-describedby="emailHelpId" placeholder="Email Address">
            </div>
            <div class="mb-8">
                <input type="submit" value="Reset Password" id="R" class="w-full px-4 py-2 text-white bg-blue-500 rounded hover:bg-blue-600 transition duration-200">
            </div>
        </form>
    </div>
</body>

</html>
